/**  This file and subsequent files in this folder are for the mongoose models to define
 *   the schema and export
 * 
 *   This file can be deleted as it was mostly meant to provide a template for what type of files should go into this folder 
 * 
 *   IE: recipeController, userController, ingredientsController
 */

// const mongoose = require('mongoose');

// mongoose model schema will go here
/**
 * const testModel = mongoose.Schema({
    testName: {
        type: String,
        required: true
    },
    testNumber: {
        type: Number,
        required: false
    }
}, {timestamps: true});

module.exports = mongoose.model('Test', testModel);
 */
